# Estimacion Bayesiana del modelo
Mediante el uso de un wrapper de Python, es posible compilar un codigo de c++ para ser llamado como una funcion tipo caja negra en Python.

Caja negra:
Aquella en donde no conocemos a priori derivadas parciales en funcion de los distintos parametros para poder ser utilizada tecnicas de optimizacion iterativas.

Para ello vease el codigo en `wsl` 