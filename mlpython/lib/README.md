# Libreria de analisis y uso del Oracle
Oracle es llamado el programa generado con make dentro de `dihiggs/`, cabe destacar que una vez lo generes debes de modificar la ruta en `oracle.py` para asi llamarlo.

## Modulos
- `explorer.py`:
- `oracle.py`:
- `utils.py`: