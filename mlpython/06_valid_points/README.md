# Pipeline
Para utilizar comeince por tener sus distintos datos e `../data_batches`, estos estaran en formato `merged_*.pkl`
Utilice entonces `06b_valid_from_data.ipynb` para crear unos csv en `./output` que contendra datos filtrados con las condiiciones de importancia.
